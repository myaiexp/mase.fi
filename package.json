{
  "name": "v2",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "build-lock vite build && cp src/base.css dist/base.css && npm run build:components",
    "preview": "vite preview",
    "test": "vitest run",
    "build:components": "build-lock vite build --config vite.components.config.js",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.2.0",
    "jsdom": "^29.0.1",
    "vite": "^7.3.1",
    "vitest": "^4.1.0"
  },
  "dependencies": {
    "@chenglou/pretext": "^0.0.7"
  }
}
