.desloppify/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Canonical package manager is npm (package-lock.json). Other PM lockfiles are
# redundant and drift in resolved versions — keep them out of the repo.
bun.lock
bun.lockb
pnpm-lock.yaml

# Dev-only fixture; production updates.json lives in nginx webroot
public/updates.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
